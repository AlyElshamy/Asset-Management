@page
@model AssetProject.Areas.Identity.Pages.Account.ChangePasswordModel
@{
     Layout = "~/Pages/Shared/Admin/_AdminLayoutSlimEmpty.cshtml";
}
<div class="d-md-flex flex-row-reverse">
    <div class="signin-right">
       
        <div class="signin-box">
             <form method="post">
                 <h4 class="signin-title-secondary">Change your password.</h4>
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="@Model.changePasswordVM.CurrentPassword"></label>
                <input asp-for="@Model.changePasswordVM.CurrentPassword" class="form-control" placeholder="Enter Your Current Password" />
                <span asp-validation-for="@Model.changePasswordVM.CurrentPassword" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.changePasswordVM.NewPassword"></label>
                <input asp-for="@Model.changePasswordVM.NewPassword" class="form-control" placeholder="Enter New Password" />
                <span asp-validation-for="@Model.changePasswordVM.NewPassword" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.changePasswordVM.ConfirmPassword"></label>
                <input asp-for="@Model.changePasswordVM.ConfirmPassword" class="form-control" placeholder="Confirm Password" />
                <span asp-validation-for="@Model.changePasswordVM.ConfirmPassword" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary btn-block btn-signin">Change</button>
        </form>
      </div>
    </div>
  
       <div class="signin-left">
        <div class="signin-box">
          <h2 class="slim-logo"><a href="index.html">slim<span>.</span></a></h2>

          <p>We are excited to launch our new company and product Slim. After being featured in too many magazines to mention and having created an online stir, we know that ThemePixels is going to be big. We also hope to win Startup Fictional Business of the Year this year.</p>

          <p>Browse our site and see for yourself why you need Slim.</p>

          <p><a href="#" class="btn btn-outline-secondary pd-x-25">Learn More</a></p>

          <p class="tx-12">&copy; Copyright 2018. All Rights Reserved.</p>
        </div>
      </div><!-- signin-left -->
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
